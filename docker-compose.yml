version: "3.8"
services:
  terminogrid:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${IMAGE:-terminogrid-backend:local}
    container_name: terminogrid
    ports:
      - "8181:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
